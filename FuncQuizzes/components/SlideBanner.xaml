<UserControl x:Class="FuncQuizzes.components.SlideBanner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FuncQuizzes.components"
             mc:Ignorable="d" 
             d:DesignHeight="218" d:DesignWidth="762">
    <Border CornerRadius="12">

        <!-- Border Background -->
        <Border.Background>
            <LinearGradientBrush EndPoint="0.5,0.9" StartPoint="0.5,0" >
                <GradientStop Color="#5787BA" Offset="0.9"/>
                <GradientStop Color="#547EA9"/>
            </LinearGradientBrush>
        </Border.Background>

        <Grid>
            <!-- Define the Column -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="400"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0">
                <Border.Background>
                    <ImageBrush ImageSource="../assets/images/CandC++.png"/>
                </Border.Background>
            </Border>

            <StackPanel Grid.Column="1"
                      VerticalAlignment="Center">
                <TextBlock Text="C Programming"
                           FontSize="38"
                           FontWeight="Bold"
                           Foreground="White"/>
                <TextBlock Text="C++ Programming"
                           FontSize="38"
                           FontWeight="Bold"
                           Foreground="White"/>
            </StackPanel>

        </Grid>

    </Border>
</UserControl>
